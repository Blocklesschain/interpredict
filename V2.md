IMPORTANT REPOSITORY CONTEXT

You are currently operating inside the EXISTING InterPredict closed-beta
repository.

This repository is the legacy V1/beta implementation.

Your assignment is to rebuild InterPredict V2 FROM THE GROUND UP inside
this repository.

DO NOT create a separate unrelated project outside this repository.

FIRST:

1. Perform a complete repository audit.
2. Map the current directory structure.
3. Inspect package.json and dependencies.
4. Inspect all Solidity contracts.
5. Inspect the current ABI.
6. Inspect Next.js configuration.
7. Inspect Netlify configuration.
8. Inspect Supabase/database-related code, if present.
9. Inspect RPC/authentication infrastructure.
10. Inspect wallet integration.
11. Inspect API routes.
12. Inspect market-fetching/indexing/cache implementation.
13. Inspect localization.
14. Inspect UI components and routes.
15. Inspect environment-variable references.
16. Inspect tests.
17. Inspect deployment scripts.
18. Identify dead, duplicated and obsolete code.

Create:

docs/LEGACY_ARCHITECTURE_AUDIT.md

Classify the existing implementation into:

REUSE
REFACTOR
REWRITE
DELETE
REFERENCE_ONLY

IMPORTANT:

"Build from scratch" does NOT mean blindly deleting the repository.

Preserve infrastructure that is already correct, secure and useful.

Examples may include:

- branding/assets;
- InterLink network configuration;
- tested RPC authentication;
- environment-variable conventions;
- wallet integration primitives;
- useful TypeScript types;
- localization assets;
- deployment configuration;
- verified contract/network information.

However, do NOT preserve architectural decisions merely because they
already exist.

The new architecture defined in this specification takes precedence.

-----------------------------------------------
SAFE REBUILD PROCEDURE
-----------------------------------------------

Before destructive modifications:

1. Run git status.
2. Record the current commit hash.
3. Confirm that the repository has a recoverable Git history.
4. Create docs/LEGACY_ARCHITECTURE_AUDIT.md.
5. Create docs/V2_REBUILD_PLAN.md.
6. Create docs/IMPLEMENTATION_PROGRESS.md.

Then rebuild the application incrementally inside this repository.

You MAY:

- replace legacy components;
- replace API routes;
- replace database architecture;
- replace indexing architecture;
- replace obsolete services;
- replace the Solidity architecture where required;
- restructure directories;
- remove dead code;
- introduce new migrations;
- rewrite UI/UX;
- replace unreliable RPC-heavy read paths.

But every deletion must have a reason.

Record removed/replaced files in:

docs/V2_REBUILD_CHANGELOG.md

For each significant legacy file record:

OLD:
path/to/file

ACTION:
REUSED | REFACTORED | REWRITTEN | DELETED

REASON:
...

REPLACEMENT:
path/to/new/file

-----------------------------------------------
DO NOT DESTROY WORKING INFRASTRUCTURE FIRST
-----------------------------------------------

Do NOT begin by deleting the old application.

Use an incremental replacement strategy:

AUDIT
  ↓
ARCHITECT
  ↓
BUILD NEW FOUNDATION
  ↓
TEST FOUNDATION
  ↓
REPLACE OLD MODULE
  ↓
TEST
  ↓
CONTINUE

At all reasonable checkpoints the repository should remain recoverable.

-----------------------------------------------
GIT SAFETY
-----------------------------------------------

You are working on the:

interpredict-v2-rebuild

branch.

DO NOT:

- force push;
- rewrite Git history;
- delete the main branch;
- delete Git tags;
- push to main;
- merge into main;
- deploy production;
- deploy Mainnet contracts;
- modify production secrets.

You may make local commits after successfully completed stages.

Use descriptive commits such as:

chore(v2): establish architecture
feat(v2): implement database schema
feat(v2): implement blockchain indexer
feat(v2): implement market read API
feat(v2): rebuild marketplace UI
test(v2): add beta regression suite

DO NOT merge into main until I explicitly authorize it.

-----------------------------------------------
LEGACY VS V2
-----------------------------------------------

Treat V1 as evidence, not authority.

When V1 behavior contradicts:

- the V2 specification;
- security requirements;
- database integrity;
- contract invariants;
- beta feedback;
- performance requirements;

V2 wins.

If you discover a major contradiction that requires a PRODUCT decision
rather than an engineering decision, STOP that specific feature,
document the question, and ask me instead of inventing business rules.

Now proceed with the complete InterPredict V2 Master Ground-Up Rebuild
Specification below.


# InterPredict V2 — Master Ground-Up Rebuild Prompt for DeepSeek-v4-Pro

## ROLE

You are the principal architect and implementation agent responsible for rebuilding **InterPredict V2 from scratch**.

Act simultaneously as:

* Principal software architect
* Senior Solidity engineer
* Senior Next.js/TypeScript engineer
* Supabase/PostgreSQL architect
* Blockchain indexing engineer
* Web3 wallet-integration engineer
* UI/UX engineer
* Application-security engineer
* QA/automation engineer
* DevOps/Netlify engineer
* Technical documentation engineer

This is a **new implementation**, not a patch of the previous beta application.

The previous InterPredict repository and beta reports may be inspected for:

* product requirements;
* successful UX concepts;
* contract concepts;
* branding;
* lessons learned;
* bugs that must not return.

Do NOT blindly copy legacy architecture or code.

---

# 1. PRODUCT OBJECTIVE

Build InterPredict as a production-quality, community-driven forecasting application for the InterLink EVM environment.

For this implementation, participation must remain testnet/non-cash and must not implement real-money wagering, financial payouts, or mechanisms for users to profit from bets.

The system should support:

1. Community-created forecasting markets
2. Multi-outcome questions
3. Market proposal workflow
4. Community curation through DEC
5. Market lifecycle management
6. Resolution requests
7. DEC resolution workflow
8. Transparent on-chain state
9. User participation records
10. Personal activity/history
11. Market discovery
12. Multilingual UI
13. Fast indexed reads
14. Reliable wallet interaction
15. Mobile-first experience
16. Administrative/operational monitoring

The application must be designed so that normal browsing is extremely fast and DOES NOT depend on reconstructing the entire application from RPC calls.

---

# 2. CORE ARCHITECTURAL PRINCIPLE

Use this architecture:

```text
                     ┌─────────────────────┐
                     │ InterLink EVM Chain │
                     └──────────┬──────────┘
                                │
                     Events / authoritative state
                                │
                                ▼
                  ┌─────────────────────────┐
                  │ Blockchain Synchronizer │
                  │ / Indexer               │
                  └────────────┬────────────┘
                               │
                        transactional upserts
                               │
                               ▼
                  ┌─────────────────────────┐
                  │ Supabase PostgreSQL     │
                  │ Indexed Read Model      │
                  └────────────┬────────────┘
                               │
                         optimized queries
                               │
                               ▼
                  ┌─────────────────────────┐
                  │ Next.js Backend APIs    │
                  └────────────┬────────────┘
                               │
                               ▼
                  ┌─────────────────────────┐
                  │ Next.js Frontend        │
                  └─────────────────────────┘
```

Transaction/state-change path:

```text
User
 ↓
UI
 ↓
Wallet signature
 ↓
Smart contract
 ↓
Transaction confirmation
 ↓
Targeted synchronization
 ↓
PostgreSQL read model
 ↓
Query invalidation/refetch
 ↓
Updated UI
```

ABSOLUTELY AVOID:

```text
Page load
 ↓
RPC call per market
 ↓
5 RPC calls per market
 ↓
dozens/hundreds of RPC requests
 ↓
partial page
```

Normal page loads should require **zero full-chain scans**.

---

# 3. PHASE ZERO — ARCHITECT BEFORE CODING

Do NOT immediately generate the application.

First create:

```text
/docs/
    PRODUCT_REQUIREMENTS.md
    SYSTEM_ARCHITECTURE.md
    SMART_CONTRACT_SPEC.md
    CONTRACT_STATE_MACHINE.md
    DATA_OWNERSHIP.md
    DATABASE_SCHEMA.md
    INDEXER_ARCHITECTURE.md
    API_SPEC.md
    UI_UX_SPEC.md
    INTERNATIONALIZATION.md
    SECURITY_MODEL.md
    THREAT_MODEL.md
    TEST_PLAN.md
    DEPLOYMENT.md
    ENVIRONMENT_VARIABLES.md
    OBSERVABILITY.md
    MIGRATION_STRATEGY.md
    ROLLBACK_PLAN.md
```

Every requirement must receive an ID:

```text
REQ-FUNC-###
REQ-CONTRACT-###
REQ-DATA-###
REQ-SYNC-###
REQ-API-###
REQ-UX-###
REQ-PERF-###
REQ-SEC-###
REQ-I18N-###
```

Maintain traceability:

```text
Requirement
→ architecture
→ implementation
→ automated test
→ result
```

---

# 4. TECHNOLOGY STACK

Prefer:

### Frontend

* Next.js App Router
* TypeScript
* React
* modern component architecture
* accessible responsive UI
* existing project-compatible wallet library

### Blockchain

* Solidity
* Hardhat
* ethers.js
* OpenZeppelin where appropriate

### Database

* Supabase PostgreSQL

### Infrastructure

* Netlify
* Netlify Functions
* Netlify Background Functions where appropriate

### Storage

Use dedicated object storage for uploaded media.

Do not store binary images directly inside PostgreSQL.

### Testing

Use the repository's appropriate modern testing stack for:

* Solidity
* TypeScript
* React
* APIs
* integration
* E2E

---

# 5. MONOREPO / PROJECT ORGANIZATION

Design a clean structure approximately like:

```text
app/
components/
contracts/
lib/
services/
repositories/
hooks/
types/
i18n/
tests/
scripts/
supabase/
docs/
public/
```

Separate:

* UI
* domain logic
* blockchain interaction
* database access
* synchronization
* API logic
* wallet interaction
* configuration

Avoid giant 1,000-line route or component files.

---

# 6. SMART CONTRACT DESIGN

Design contracts from explicit invariants before implementation.

The forecasting lifecycle should include clearly defined states such as:

```text
Proposed
DECReview
Rejected
Cancelled
Approved
Active
Closed
Unresolved
ResolutionRequested
DECResolutionVoting
AdminVerification
Confirmed
Finalized
Resolved
```

Do NOT assume these exact numeric enum values until the new contract specification establishes them.

Document every transition.

Example:

```text
Proposed
   ↓
DECReview
 ↙     ↘
Rejected Approved
            ↓
          Active
            ↓
          Closed
            ↓
        Unresolved
            ↓
   ResolutionRequested
            ↓
    DECResolutionVoting
            ↓
     AdminVerification
            ↓
         Confirmed
            ↓
         Finalized
            ↓
         Resolved
```

For every state document:

* who may enter it;
* valid previous states;
* permitted actions;
* prohibited actions;
* emitted events;
* expected UI behavior.

---

# 7. CONTRACT EVENTS

Every important transition MUST emit an indexable event.

Examples conceptually:

```solidity
MarketProposed(...)
MarketApproved(...)
MarketRejected(...)
MarketCancelled(...)
MarketActivated(...)
ParticipationRecorded(...)
ResolutionRequested(...)
ResolutionVoteCast(...)
ResolutionConfirmed(...)
MarketFinalized(...)
DECMemberJoined(...)
```

Event structures must contain enough indexed information for the backend indexer to reconstruct relevant read models without making enormous numbers of secondary RPC calls.

---

# 8. CONTRACT ERRORS

Use custom errors where appropriate.

Never force the frontend to interpret mysterious generic reverts.

Examples conceptually:

```solidity
error InvalidMarketState();
error AlreadyParticipated();
error ResolutionAlreadyRequested();
error Unauthorized();
error InvalidOutcome();
error MarketClosed();
```

Frontend must translate these into understandable messages.

---

# 9. CONTRACT SECURITY

Audit for:

* reentrancy;
* access control;
* authorization bypass;
* state-transition errors;
* duplicate actions;
* integer precision;
* overflow assumptions;
* unbounded loops;
* denial-of-service vectors;
* front-running assumptions;
* replay behavior;
* emergency controls;
* storage collisions if upgradeability exists.

Use established OpenZeppelin primitives where appropriate.

Do not reinvent security primitives unnecessarily.

---

# 10. CONTRACT TESTING

Create comprehensive:

* unit tests;
* state-transition tests;
* permission tests;
* invalid-state tests;
* boundary tests;
* fuzz tests;
* invariant tests;
* event-emission tests.

Explicitly test:

```text
duplicate proposal action
duplicate participation
duplicate resolution request
unauthorized DEC action
invalid outcome
expired market
incorrect state
cancelled wallet transaction
reverted transaction
```

No contract should be considered Mainnet-ready merely because it compiles.

---

# 11. DATA OWNERSHIP

Create `/docs/DATA_OWNERSHIP.md`.

For EVERY important field define:

| Field            | Authoritative Source | DB Copy | Update Trigger  |
| ---------------- | -------------------- | ------- | --------------- |
| market ID        | Chain                | Yes     | event           |
| creator          | Chain                | Yes     | event           |
| question         | defined architecture | Yes     | creation        |
| state            | Chain                | Yes     | state event     |
| thumbnail        | metadata storage     | URL     | metadata update |
| user activity    | Chain                | Yes     | event           |
| transaction hash | Chain                | Yes     | confirmation    |

Never allow frontend state, database state and blockchain state to silently disagree without reconciliation.

---

# 12. SUPABASE DATABASE

Supabase/PostgreSQL becomes the application's **query-optimized read model**.

Do NOT store everything in:

```text
snapshot.json
```

Create normalized tables based on actual requirements.

Likely domains include:

```text
markets
market_outcomes
market_state_history
user_activity
proposal_votes
resolution_requests
resolution_votes
dec_members
transactions
sync_checkpoints
sync_failures
categories
supported_locales
```

Final schema must be derived from the contract/product specification rather than blindly copying this list.

---

# 13. DATABASE PROVENANCE

Chain-derived rows should retain relevant provenance:

```text
chain_id
contract_address
block_number
block_hash
transaction_hash
log_index
indexed_at
created_at
updated_at
```

This makes debugging and reconciliation possible.

---

# 14. DATABASE INDEXING

Create indexes based on actual query patterns.

Potential indexes:

```text
market ID
state
creator
category
created timestamp
end timestamp
wallet address
transaction hash
block number
```

Use composite indexes where query patterns justify them.

Use `EXPLAIN ANALYZE` for important queries where appropriate.

Avoid N+1 database access.

---

# 15. DATABASE SECURITY

Configure:

* Row Level Security where appropriate;
* PostgreSQL grants;
* least privilege;
* server-only administrative access.

Never expose the Supabase service/secret credential to browser code.

Never name server secrets:

```text
NEXT_PUBLIC_*
```

---

# 16. BLOCKCHAIN INDEXER

Create a dedicated synchronization layer.

It must be:

* incremental;
* resumable;
* idempotent;
* observable;
* retry-safe.

Maintain:

```text
sync_checkpoints
```

containing conceptually:

```text
chain_id
contract_address
last_processed_block
last_processed_block_hash
last_successful_sync_at
sync_status
last_error
```

---

# 17. INCREMENTAL SYNCHRONIZATION

The indexer should:

```text
Read checkpoint
↓
Determine safe confirmed block
↓
Determine missing range
↓
Read bounded range
↓
Decode relevant events
↓
Validate
↓
Upsert transactionally
↓
Commit
↓
Advance checkpoint
```

If persistence fails:

**DO NOT ADVANCE THE CHECKPOINT.**

---

# 18. IDEMPOTENCY

Reprocessing an event must not duplicate records.

Use appropriate uniqueness such as:

```text
chain_id
+ transaction_hash
+ log_index
```

where appropriate.

---

# 19. RPC FAILURE HANDLING

Handle:

* 429/rate limiting;
* 500 errors;
* network timeout;
* malformed response;
* unavailable RPC;
* expired authentication;
* partial batch failure.

Use:

* bounded concurrency;
* exponential backoff;
* jitter;
* maximum retries;
* structured logging.

NEVER convert an RPC failure into legitimate-looking:

```text
0
[]
""
false
```

and permanently overwrite known-good data.

---

# 20. BACKGROUND SYNCHRONIZATION

The expensive synchronization process must be independent from normal page requests.

Preferred:

```text
Netlify Background Function
        ↓
Indexer
        ↓
Supabase
```

A normal request should be:

```text
Browser
 ↓
API
 ↓
PostgreSQL
 ↓
Response
```

NOT:

```text
Browser
 ↓
API
 ↓
Blockchain scan
 ↓
wait
```

---

# 21. TARGETED RECONCILIATION

After a confirmed state-changing transaction:

```text
receipt
 ↓
affected market/entity IDs
 ↓
targeted synchronization
 ↓
database update
 ↓
invalidate relevant frontend queries
 ↓
updated UI
```

Do NOT rescan every market because one record changed.

---

# 22. API ARCHITECTURE

Create typed APIs appropriate to the final product.

Conceptually:

```text
GET /api/markets
GET /api/markets/:id
GET /api/markets?state=
GET /api/markets?category=
GET /api/activity
GET /api/users/:wallet/activity
GET /api/dec
GET /api/health
```

Use:

* pagination;
* filtering;
* sorting;
* selective columns;
* input validation.

Avoid returning unlimited datasets.

---

# 23. STANDARD API ENVELOPE

Use consistent responses:

```json
{
  "data": {},
  "meta": {},
  "error": null
}
```

Errors should contain safe machine-readable codes.

---

# 24. FIX THE BETA MARKET-LOADING FAILURE

Beta testers repeatedly reported:

* partial markets;
* created markets missing;
* active markets missing;
* stale market counts;
* browser history showing different information from a fresh tab;
* slow proposal pages.

The new architecture MUST eliminate request-time full-market RPC reconstruction.

Marketplace data must come from PostgreSQL.

Add synchronization freshness metadata internally.

Do not silently show stale state indefinitely.

---

# 25. FIX "MY VOTES"

Beta testers successfully participated but sometimes saw:

> You haven't placed any predictions yet.

Audit the new architecture so personal activity is indexed deterministically.

Normalize EVM addresses consistently.

Never depend on case-sensitive wallet comparison.

Test:

```text
wallet participates
→ confirmation
→ event indexed
→ DB activity created
→ My Votes refetch
→ participation visible
```

Test reconnecting the same wallet.

---

# 26. BALANCE SYNCHRONIZATION

Beta testers reported approximately one-minute delays.

After transaction confirmation:

* invalidate/refetch relevant balance;
* update relevant user state;
* reconcile against authoritative source;
* show confirmed status.

Do not wait for a long global polling interval.

---

# 27. MARKET CREATION VISIBILITY

Beta testers created markets successfully but could only see the transaction in history.

Required:

```text
successful creation
→ receipt
→ event
→ targeted synchronization
→ proposal appears immediately
```

Provide states visibly:

```text
Pending
Under Review
Approved
Active
Rejected
Closed
Under Resolution
Resolved
```

Use terminology established by the final specification.

---

# 28. DEC WORKFLOW

Build a clear DEC experience.

Users should understand:

* eligibility;
* membership status;
* available proposals;
* voting state;
* resolution state;
* completed activity.

Do not allow buttons for invalid actions.

If resolution has already been requested:

DO NOT display active:

```text
REQUEST RESOLUTION
```

Instead display an appropriate disabled/current-state label.

---

# 29. DYNAMIC ACTION ENGINE

Centralize action availability.

Create something conceptually like:

```text
getAvailableActions(marketState, userContext)
```

Do not duplicate state logic across dozens of React components.

Return:

```text
visible
enabled
label
reasonDisabled
```

for each action.

---

# 30. ERROR NORMALIZATION

Beta testers saw:

```text
SERVER_ERROR 500
CALL_EXCEPTION
UNKNOWN_ERROR
eth_sendTransaction
missing revert data
```

Users must NEVER see these raw errors.

Create a central normalization layer.

Examples:

```text
USER_CANCELLED
RPC_RATE_LIMITED
NETWORK_UNAVAILABLE
INVALID_STATE
ACTION_ALREADY_COMPLETED
TRANSACTION_REVERTED
INDEXING_DELAY
WALLET_DISCONNECTED
UNKNOWN
```

Display friendly messages.

Technical diagnostics belong in logs.

---

# 31. ERROR LIFECYCLE

Beta testers reported error banners persisting across unrelated pages.

Fix this.

Transient errors should clear appropriately on:

* navigation;
* successful retry;
* new transaction;
* timeout where appropriate;
* component teardown.

Do not globally persist unrelated transactional errors.

---

# 32. WALLET UX

Handle:

* connection;
* disconnection;
* account switching;
* network switching;
* wallet rejection;
* transaction pending;
* transaction confirmed;
* transaction reverted.

Never interpret wallet cancellation as a protocol failure.

---

# 33. MOBILE SESSION RESTORATION

Beta testers reported:

> lock/minimize phone → return → application resets to wallet/home.

Preserve appropriate navigation/session state.

When returning from:

* wallet application;
* phone lock;
* app minimization;
* tab suspension;

restore the intended route where possible.

Then revalidate potentially stale data.

Never persist wallet secrets.

---

# 34. NAVIGATION

Beta feedback requested better navigation.

Primary destinations must be immediately discoverable.

Include clearly accessible navigation for core areas such as:

```text
Home
Marketplace
Market Proposals
My Activity
Create
DEC
History
Help
```

Do not bury every primary action inside a dropdown.

---

# 35. BACK BUTTON

Fix beta behavior where Back unexpectedly returned users to the application homepage.

Use normal browser/router history semantics.

Test:

```text
Home → Marketplace → Market → Back
```

Expected:

```text
Marketplace
```

not forced Home.

Test deep links separately.

---

# 36. HOME ACCESS

Provide an obvious Home action in responsive navigation where useful.

It must not interfere with normal Back behavior.

---

# 37. MARKETPLACE

Design responsive market cards.

Support:

* Active
* Upcoming/proposed where appropriate
* Closed
* Resolved

Provide filters.

Potential categories should be data-driven rather than hard-coded throughout components.

---

# 38. ACTIVE MARKET PRESENTATION

Evaluate:

* responsive card grids;
* horizontally scrollable featured sections;
* carousel only where genuinely useful.

Never hide important content behind an inaccessible carousel.

Support:

* keyboard;
* touch;
* mobile;
* reduced motion where appropriate.

---

# 39. MARKET THUMBNAILS

Beta testers reported distortion.

Define a media specification.

For example:

```text
recommended aspect ratio
maximum upload size
supported MIME types
fallback image
```

Use proper:

```css
object-fit
aspect-ratio
```

behavior.

Never stretch images unnaturally.

---

# 40. MARKET DETAIL PAGE

Present clearly:

* question;
* description;
* category;
* status;
* outcomes;
* creator;
* relevant timestamps;
* resolution criteria;
* activity;
* appropriate actions.

Resolution criteria must be visible BEFORE user participation.

---

# 41. MARKET CREATION UX

Use a guided form.

Include:

* clear fields;
* validation;
* category selection;
* outcome configuration;
* thumbnail preview;
* resolution criteria;
* timezone explanation;
* final review screen.

Before submission show a confirmation dialog summarizing what will be submitted.

---

# 42. UTC / LOCAL TIME

Beta testers found time semantics confusing.

Internally use a canonical timestamp representation.

UI should clearly display:

```text
Your local time
UTC equivalent
```

before final submission.

Store/compare timestamps consistently.

Test DST/timezone edge cases where relevant.

---

# 43. INTERNATIONALIZATION

Beta testers saw mixed languages.

Fix completely.

Centralize all user-visible strings.

No hard-coded English scattered across components.

Create locale files.

Initially preserve required existing locales and make architecture extensible for additional languages such as Vietnamese.

When a language changes:

**the entire interface must change consistently.**

Provide deterministic fallback behavior.

Persist preference.

---

# 44. DESIGN SYSTEM

Create reusable primitives:

```text
Button
Input
Select
Card
Modal
Toast
Badge
Tabs
Tooltip
Skeleton
EmptyState
ErrorState
TransactionStatus
```

Define consistent:

* spacing;
* typography;
* radius;
* shadows;
* borders;
* hover;
* focus;
* disabled;
* loading;
* success;
* warning;
* error.

---

# 45. BUTTON DESIGN

Beta feedback specifically requested more polished buttons.

Create consistent variants:

```text
primary
secondary
outline
ghost
danger
```

Ensure accessible touch targets.

Every async button should support:

```text
idle
loading
success
disabled
```

Prevent accidental duplicate submissions.

---

# 46. THEMES

Implement:

* dark;
* light;
* system.

Persist user preference.

Avoid flash of incorrect theme.

Test accessibility/contrast in both themes.

---

# 47. LOADING UX

Never leave users wondering whether the application froze.

Use:

* skeletons;
* local spinners;
* transaction pending states;
* synchronization indicators where genuinely useful.

Avoid full-page blocking loaders for small operations.

---

# 48. EMPTY STATES

Differentiate:

```text
No records exist
```

from:

```text
Data failed to load
```

from:

```text
Indexer temporarily behind
```

from:

```text
Wallet has no activity
```

These are NOT the same state.

---

# 49. USER GUIDANCE

Beta testers requested detailed instructions.

Create:

```text
/help
```

with concise explanations of:

* creating forecasting markets;
* market states;
* DEC;
* resolution;
* timestamps;
* transaction lifecycle;
* common errors.

Use contextual tooltips where appropriate.

---

# 50. SUPPORT / ISSUE REPORTING

Provide an accessible issue-reporting entry point.

Allow users to report:

* page;
* category;
* description.

Optionally include safe diagnostic context.

NEVER collect:

* private key;
* seed phrase;
* wallet secrets;
* authentication tokens.

---

# 51. CACHE STRATEGY

PostgreSQL is the durable indexed read model.

Optional cache may sit ABOVE PostgreSQL.

Never let cache become the authoritative source.

Define:

* TTL;
* invalidation;
* stale behavior;
* post-transaction invalidation.

Eliminate beta behavior where browser history and fresh tabs show conflicting market counts.

---

# 52. PERFORMANCE BUDGET

Establish measurable targets.

Track:

* API p50;
* API p95;
* DB query p95;
* page LCP;
* indexer lag;
* RPC failure rate;
* transaction-confirmation-to-UI-update delay.

Normal market page:

```text
0 full-chain scans
0 per-market RPC fan-out
```

---

# 53. DATABASE QUERY PERFORMANCE

Use pagination.

Do not fetch 10,000 rows when displaying 20.

Use indexed filters.

Select only required columns.

Avoid N+1 queries.

---

# 54. OBSERVABILITY

Implement structured logging.

Include:

```text
request_id
correlation_id
operation
duration
status
```

Indexer logs should include:

```text
start_block
end_block
events_processed
records_upserted
retry_count
duration
checkpoint
```

Never log secrets.

---

# 55. HEALTH ENDPOINT

Create a safe health endpoint.

Conceptually return:

```json
{
  "status": "ok",
  "database": "reachable",
  "indexer": {
    "status": "healthy",
    "lastSuccessfulSync": "...",
    "lag": "..."
  }
}
```

Do not expose credentials or sensitive infrastructure details.

---

# 56. NETLIFY ENVIRONMENT

Generate an exact environment-variable checklist after architecture is finalized.

Likely server variables include equivalents of:

```text
SUPABASE_URL
SUPABASE_SECRET_KEY

INTERLINK_RPC_URL
INTERLINK_BACKEND_PRIVATE_KEY

CONTRACT_ADDRESS

INDEXER_SECRET
INDEXER_START_BLOCK
INDEXER_BATCH_SIZE
INDEXER_CONFIRMATION_DEPTH
```

Reuse existing authentication helpers only if they remain appropriate after audit.

Never hard-code secrets.

---

# 57. SUPABASE SETUP

Generate SQL migrations.

Do not ask me to manually recreate tables one by one.

Provide:

```text
supabase/migrations/
```

for:

* schema;
* indexes;
* constraints;
* RLS;
* functions/triggers where justified.

Provide exact setup documentation.

---

# 58. SECRET MANAGEMENT

Strictly separate:

### Public configuration

safe for browser.

### Server secrets

never bundled into browser.

Fail startup/deployment when required secrets are missing rather than silently using unsafe defaults.

---

# 59. SECURITY HEADERS

Configure appropriate:

* CSP;
* frame protection;
* MIME protections;
* referrer policy;
* permissions policy.

Ensure CSP remains compatible with wallet integrations and required external resources.

---

# 60. INPUT VALIDATION

Validate ALL untrusted input server-side.

Use schemas.

Validate:

* IDs;
* wallet addresses;
* categories;
* pagination;
* strings;
* uploaded files;
* query parameters.

Never rely exclusively on frontend validation.

---

# 61. RATE LIMITING

Protect expensive APIs.

Especially protect:

* synchronization triggers;
* upload endpoints;
* issue reports;
* administrative routes.

---

# 62. ADMINISTRATIVE ROUTES

Administrative endpoints must never rely on obscure URLs for security.

Use proper authorization.

Protect manual indexer refresh.

Never expose:

```text
POST /api/indexer/refresh
```

publicly without authentication.

---

# 63. TESTING STRATEGY

Create comprehensive automated tests.

### Contracts

* unit
* fuzz
* invariants
* permissions
* state machine

### Database

* constraints
* idempotent upserts
* migrations

### Indexer

* duplicate events
* restart
* checkpoint recovery
* RPC failure
* DB failure
* reprocessing

### APIs

* validation
* pagination
* errors
* authorization

### Frontend

* components
* navigation
* responsive behavior
* loading/error states

### E2E

Full user journeys.

---

# 64. REQUIRED E2E TESTS

Test at minimum:

```text
connect wallet
disconnect wallet
switch account
create proposal
proposal becomes visible
DEC workflow
market state transitions
participation recorded
My Activity/My Votes updates
request resolution
resolution workflow
final state
balance/state refresh
wallet cancellation
RPC outage
RPC rate limit
database outage
indexer delay
duplicate event
browser refresh
browser history
phone suspension/resume
mobile navigation
language switch
theme switch
thumbnail rendering
timezone conversion
```

---

# 65. BETA REGRESSION SUITE

Create tests specifically named after historical beta failures.

Examples:

```text
regression_my_votes_missing
regression_created_market_missing
regression_partial_market_loading
regression_stale_browser_cache
regression_balance_delay
regression_resolution_button_invalid
regression_raw_rpc_error
regression_global_error_persistence
regression_mixed_localization
regression_back_navigation
regression_mobile_session_reset
regression_thumbnail_distortion
```

Every confirmed beta defect must have a regression test wherever technically feasible.

---

# 66. FAILURE TESTING

Simulate:

* RPC offline;
* RPC slow;
* RPC 429;
* RPC 500;
* Supabase offline;
* transaction rejected;
* transaction reverted;
* indexer crash mid-batch;
* duplicated event;
* malformed metadata;
* slow 3G;
* stale cache.

Application must degrade gracefully.

---

# 67. CI

Before merging:

```text
lint
typecheck
contract compile
contract tests
unit tests
integration tests
E2E critical path
production build
dependency/security checks
```

Critical failure = failed pipeline.

Do not suppress errors merely to obtain a green build.

---

# 68. DEVELOPMENT QUALITY RULES

Use strict TypeScript.

Avoid `any`.

Avoid enormous components.

Avoid duplicated domain logic.

Avoid magic numbers.

Use typed configuration.

Document unusual decisions.

Do not over-engineer trivial functionality.

---

# 69. DO NOT TRUST SUCCESS TOASTS

A frontend saying:

```text
SUCCESS
```

does not prove state correctness.

Validate against:

* transaction receipt;
* expected emitted event;
* indexed record;
* resulting application state.

---

# 70. SOURCE-OF-TRUTH MATRIX

Before declaring completion, create:

```text
/docs/SOURCE_OF_TRUTH.md
```

Resolve contradictions between:

* Solidity;
* ABI;
* frontend;
* database;
* documentation.

There must be ONE defined authority for every important rule.

---

# 71. DOCUMENTATION CONSISTENCY

Beta testers found documentation inconsistencies.

Create versioned documentation.

Display one canonical version.

Do not allow README, UI and protocol specification to contradict each other.

Add an automated or manual release checklist for documentation consistency.

---

# 72. MIGRATION / LEGACY CODE

Although this is a ground-up rebuild, inspect the old project for useful assets.

Classify old code:

```text
REUSE
REWRITE
DISCARD
REFERENCE ONLY
```

Do not copy old technical debt into V2.

---

# 73. DEPLOYMENT ENVIRONMENTS

Maintain:

```text
local
development
preview
testnet
production
```

Configuration must not accidentally mix environments.

Never allow testnet contract configuration to silently become production configuration.

---

# 74. DATABASE BACKFILL

If existing testnet data needs to appear in the rebuilt application, write a dedicated backfill/indexing script.

Do not mix historical backfill with normal request handling.

Backfill must be:

* resumable;
* observable;
* idempotent.

---

# 75. DEPLOYMENT PROCESS

Do not deploy automatically.

Before deployment provide:

```text
BUILD STATUS
TEST STATUS
SECURITY STATUS
DATABASE MIGRATION STATUS
CONTRACT STATUS
INDEXER STATUS
ENVIRONMENT STATUS
KNOWN ISSUES
ROLLBACK PROCEDURE
```

Ask me for approval.

---

# 76. CONTRACT DEPLOYMENT

Before any contract deployment:

provide:

* compiler version;
* optimizer settings;
* constructor arguments;
* expected contract address workflow;
* network;
* deployment account requirements;
* verification instructions;
* ABI generation process;
* frontend configuration changes;
* rollback/migration implications.

Do not deploy without explicit approval.

---

# 77. DATABASE MIGRATION SAFETY

Every production migration should be reviewed for:

* destructive operations;
* locks;
* backwards compatibility;
* rollback.

Prefer additive migrations during rollout.

---

# 78. ZERO-DOWNTIME DATA MIGRATION

Use staged rollout:

### Phase A

Deploy database/indexer.

### Phase B

Backfill.

### Phase C

Validate parity.

### Phase D

Enable DB read API.

### Phase E

Switch frontend reads.

### Phase F

Monitor.

### Phase G

Remove obsolete code only after confidence period.

---

# 79. PARITY VALIDATION

Before switching reads, compare sampled records:

```text
chain
vs
database
vs
API
vs
UI
```

Verify:

* ID;
* state;
* creator;
* outcomes;
* timestamps;
* activity.

Produce a parity report.

---

# 80. RELEASE READINESS

Generate:

```text
/docs/RELEASE_READINESS.md
```

with:

| Requirement | Implementation | Test | Status | Evidence |
| ----------- | -------------- | ---- | ------ | -------- |

Statuses:

```text
PASS
FAIL
BLOCKED
NOT IMPLEMENTED
```

Critical requirements may not be FAIL/BLOCKED/NOT IMPLEMENTED for release approval.

---

# 81. SECURITY REVIEW GATE

Before production recommendation:

perform a final internal security review of:

* contracts;
* permissions;
* API authorization;
* secrets;
* database RLS;
* RPC authentication;
* upload handling;
* admin routes;
* dependency vulnerabilities;
* frontend leakage.

Clearly state that internal automated review does not replace an independent professional audit for high-risk production contracts.

---

# 82. PERFORMANCE REPORT

Produce measurable before/after architecture results.

Include:

```text
RPC requests per page
API latency
DB latency
indexer sync duration
indexer lag
page load metrics
post-transaction refresh time
```

Do not merely say:

> performance improved.

Provide evidence.

---

# 83. UX ACCEPTANCE

Verify at:

```text
320px
375px
390px
430px
768px
1024px
1440px+
```

Check:

* overflow;
* navigation;
* cards;
* dialogs;
* forms;
* wallet controls;
* buttons;
* thumbnails;
* localization.

---

# 84. ACCESSIBILITY

Use:

* semantic HTML;
* labels;
* focus states;
* keyboard navigation;
* ARIA only where necessary;
* sufficient contrast;
* accessible dialogs;
* reduced-motion support where appropriate.

---

# 85. FINAL DELIVERY REPORT

When implementation is complete, return:

## Architecture

Explain final architecture.

## Contracts

List contracts and responsibilities.

## Database

List tables, relationships and indexes.

## Indexer

Explain synchronization and recovery.

## APIs

List endpoints.

## UI

List major screens/components.

## Beta Issues

For EVERY beta issue:

```text
issue
root cause
fix
test
status
```

## Files

Provide:

```text
created
modified
deleted
```

## Environment Variables

List every required variable and whether:

```text
public
server secret
build-time
runtime
```

## Supabase

Provide exact setup/migration instructions.

## Netlify

Provide exact deployment configuration.

## Tests

Show test commands and results.

## Performance

Provide measurements.

## Security

Provide findings and remaining risks.

## Known Limitations

Do not hide them.

## Deployment

Provide step-by-step deployment procedure.

## Rollback

Provide rollback procedure.

---

# 86. WORKING METHOD FOR DEEPSEEK-v4-PRO

Do NOT attempt to implement the entire system in one uncontrolled generation.

Work in stages.

After every major stage:

1. inspect existing work;
2. implement;
3. typecheck;
4. test;
5. review;
6. document;
7. summarize.

Maintain:

```text
/docs/IMPLEMENTATION_PROGRESS.md
```

Format:

```text
PHASE
STATUS
FILES
TESTS
ISSUES
NEXT ACTION
```

---

# 87. REQUIRED IMPLEMENTATION ORDER

Follow this order:

```text
01 Requirements
02 Architecture
03 Threat model
04 Smart-contract specification
05 Contract implementation
06 Contract tests
07 Database architecture
08 Supabase migrations
09 Indexer
10 Indexer tests
11 Backend repositories/services
12 APIs
13 API tests
14 Design system
15 Navigation/layout
16 Wallet integration
17 Market/proposal UI
18 DEC UI
19 Personal activity UI
20 Resolution UI
21 Internationalization
22 Theme system
23 Help/support
24 Mobile/session restoration
25 Error normalization
26 Performance optimization
27 Integration testing
28 E2E regression testing
29 Security review
30 Documentation
31 Deployment readiness
```

Do not jump directly to UI before the domain/state architecture is established.

---

# 88. CRITICAL LESSON FROM THE PREVIOUS PROJECT

The previous development experience demonstrated that a seemingly minor bug can become a serious production problem.

Therefore:

**NEVER knowingly ship a critical unresolved defect simply because it appears unlikely to occur.**

When a security-sensitive issue is discovered:

```text
document
↓
reproduce
↓
classify severity
↓
fix
↓
regression test
↓
review
```

No shortcuts.

---

# 89. DEFINITION OF DONE

A feature is NOT done because:

```text
the UI exists
```

or:

```text
the transaction succeeds once
```

or:

```text
TypeScript compiles
```

A feature is done when:

```text
requirements defined
+
implementation complete
+
failure cases handled
+
tests passing
+
security reviewed
+
responsive behavior verified
+
documentation updated
+
observability available
```

---

# 90. FINAL INSTRUCTION

Build InterPredict V2 as a clean implementation from the ground up.

Prioritize in this order:

1. correctness
2. security
3. data integrity
4. reliability
5. performance
6. usability
7. maintainability
8. visual polish

Do not preserve a legacy implementation merely because replacing it is difficult.

Do not claim perfection.

Instead, produce evidence that each requirement works.

Do not push to GitHub, deploy contracts, modify production Supabase, change Netlify production configuration, or launch production automatically.

Complete the implementation and validation first.

Then present:

**INTERPREDICT V2 — RELEASE READINESS REPORT**

and wait for my explicit approval before production deployment.